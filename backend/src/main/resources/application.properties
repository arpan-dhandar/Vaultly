# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expensedb?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=MySQLpass@Arpan-99
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Updated to the modern dialect to remove warnings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Recommended to disable this warning unless you're specifically using it
spring.jpa.open-in-view=false

# Server port
server.port=8080